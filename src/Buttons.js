import React from "react";

const Buttons = props => {
  return (
    <div class="btn">
      <button className="Start_Pause">Start/Pause</button>
      <button className="Reset">Reset</button>
    </div>
  );
};

export default Buttons;
